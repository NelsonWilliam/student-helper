Novas tabelas:

```
grades
 parent_id	// Id da pasta (disciplina)
 name		// Nome da nota (p. ex. "Prova 2")
 weight		// Peso da nota pra cálculo da média
 value		// Valor que a pessoa tirou

absences
 parent_id	// Id da pasta (disciplina)
 missed_classes	// Número de faltas, pra calcular a % de faltas
 total_classes	// Número total de aulas, pra calcular a % de faltas
 ```
